[package]
name = "client"
version = "0.1.0"
edition = "2021"

[features]
default = []
window = []
audio = ["dep:rustysynth"]

[dependencies]
num-bigint = "0.4"
rustysynth = { version = "1.3", optional = true }
