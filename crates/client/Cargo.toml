[package]
name = "client"
version = "0.1.0"
edition = "2021"

[features]
default = []
window = ["dep:winit", "dep:softbuffer"]
audio = ["dep:rustysynth", "dep:cpal"]

[dependencies]
bzip2 = "0.4"
cpal = { version = "0.18", optional = true }
flate2 = "1"
num-bigint = "0.4"
rustysynth = { version = "1.3", optional = true }
softbuffer = { version = "0.4", optional = true }
winit = { version = "0.30", optional = true }
